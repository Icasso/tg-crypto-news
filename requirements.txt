# Full development requirements file
# For production deployment, use requirements-prod.txt instead

# Include production dependencies
-r requirements-prod.txt

# Development and testing dependencies (not needed in production)
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.12.0

# Code quality tools (not needed in production)
black==24.10.0
flake8==7.1.1
mypy==1.13.0

# Type stubs (not needed in production)
types-PyYAML==6.0.12.20240917
types-requests==2.32.0.20241016 